# dot-files
```
ssh root@[IP_ADDRESS_OF_REMOTE_MACHINE]

adduser henry
usermod -aG sudo henry

cmd-d

ssh henry@[IP_ADDRESS_OF_REMOTE_MACHINE]

git clone https://github.com/HenryDashwood/dot-files.git

sh dot-files/setup.sh

cmd-d

ssh henry@[IP_ADDRESS_OF_REMOTE_MACHINE]

sh dot-files/setup.sh
```
